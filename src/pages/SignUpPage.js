// Components
import { Main, SignUpContent } from '../components';

export const SignUpPage = () => {
    return (
        <Main auth = 'signup'>
            <SignUpContent />
        </Main>
    );
};
