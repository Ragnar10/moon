// Components
import { Main, SignUpWallet } from '../components';

export const WalletPage = () => {
    return (
        <Main  auth = 'wallet'>
            <SignUpWallet />
        </Main>
    );
};
