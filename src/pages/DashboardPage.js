// Components
import { MainDashboard, DashboardContent } from '../components';

export const DashboardPage = () => {
    return (
        <MainDashboard>
            <DashboardContent />
        </MainDashboard>
    );
};
