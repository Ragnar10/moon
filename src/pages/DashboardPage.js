// Components
import { Main, DashboardContent } from '../components';

export const DashboardPage = () => {
    return (
        <Main auth = 'auth'>
            <DashboardContent />
        </Main>
    );
};
