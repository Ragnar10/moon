// Components
import { Main, PresentationContent } from '../components';

export const MainPage = () => {
    return (
        <Main auth = 'auth'>
            <PresentationContent />
        </Main>
    );
};
