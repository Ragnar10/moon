// Styles
import Styles from './styles.module.scss';
// Components

const PopupLogin = () => {
    return (
        <section className = { Styles.wrapper }>

        </section>
    );
};

export default PopupLogin;
