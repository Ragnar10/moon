// Styles
import Styles from './styles.module.scss';
// Components

const PopupLogin = () => {
    return (
        <section className = { Styles.popup }>

        </section>
    );
};

export default PopupLogin;
