// Styles
import Styles from './styles.module.scss';
// Components

const Footer = () => {
    return (
        <footer className = { Styles.wrapper }>

        </footer>
    );
};

export default Footer;
