// Styles
import Styles from './styles.module.scss';
// Components

const Header = () => {
    return (
        <header className = { Styles.wrapper }>

        </header>
    );
};

export default Header;
