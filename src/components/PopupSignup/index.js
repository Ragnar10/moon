// Styles
import Styles from './styles.module.scss';
// Components

const PopupSignup = () => {
    return (
        <section className = { Styles.wrapper }>

        </section>
    );
};

export default PopupSignup;
