// Styles
import Styles from './styles.module.scss';

const Loader = () => {
    return (
        <div className = { Styles.loader }>
            <span />
            <span />
            <span />
            <span />
            <span />
        </div>
    );
};

export default Loader;
