// Styles
import Styles from './styles.module.scss';
// Components

const Content = () => {
    return (
        <section className = { Styles.wrapper }>

        </section>
    );
};

export default Content;
